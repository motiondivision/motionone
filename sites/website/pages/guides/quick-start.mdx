import { AuthDocTemplate } from "../../components/AuthDocTemplate"
import { InlineSandbox } from "../../components/template/Sandbox"
import styled from "styled-components"
export default AuthDocTemplate

export const meta = {
  title: "Quick start",
  description: "Install Motion One and make your first animation.",
  needsSponsorship: false,
  shareImage: "social/share-quick-start.png",
}

export const box = {
  html: `<div id="box"></div>`,
  css: `body {
  display: flex;
  justify-content: center;
  align-items: center;
}
#box {
  width: 100px;
  height: 100px;
  border-radius: 10px;
  background-color: var(--splash);
}`,
}

## Introduction

Motion One is the smallest fully-featured animation library for the web.

It's built on the Web Animations API, so it can run some animations off the main JavaScript thread. These animations will remain smooth even while your website is busy rendering or processing.

By the end of this quick guide, you'll have installed Motion One and created your first animation.

## Install

Motion One can be installed via npm:

```bash
npm install motion
```

Alternatively, **the examples on this page are editable**, so you can play around with them live in-browser.

## Create an animation

Import the `animate` function:

```javascript
import { animate } from "motion"
```

`animate` can animate one or more HTML/SVG elements. These elements can be provided either as a CSS selector:

```javascript
animate(".box", { backgroundColor: "red" })
```

Or provided directly, as an element or array of elements:

```javascript
const boxes = document.querySelectorAll(".box")

animate(boxes, { backgroundColor: "red" })
```

### Animatable values

`animate` is used to animate any CSS style from its current value to those provided:

<InlineSandbox
  content={{
    ...box,
    js: `animate(
  "#box",
  { transform: "rotate(45deg)" },
  { duration: 0.5 }
)`,
  }}
  showTabs={false}
/>

Additionally, it can also be used to animate individual transforms:

<InlineSandbox
  content={{
    ...box,
    js: `animate(
  "#box",
  { x: 100, rotate: 45 },
  { duration: 0.5 }
)`,
  }}
  showTabs={false}
/>

### Options

Options are passed as `animate`'s last argument. They can be used to change most things about the animation, like `duration`, `delay` and `easing`:

<InlineSandbox
  content={{
    ...box,
    js: `animate(
  "#box",
  { rotate: 90 },
  {
    duration: 0.5,
    easing: "ease-in-out",
    repeat: 3,
    direction: "alternate"
  }
)`,
  }}
  showTabs={false}
/>

### Keyframes

Values can also be set as arrays, to define a series of keyframes.

By default, keyframes are spaced evenly throughout `duration`, but this can be adjusted by providing progress values to `offset`:

<InlineSandbox
  content={{
    ...box,
    js: `animate(
  "#box",
  { x: [0, -100, 100, 0] },
  {
    duration: 2,
    offset: [0, 0.25, 0.75]
  }
)`,
  }}
  showTabs={false}
/>

### Stagger

When animating multiple elements, it can feel more natural or lively to offset the animations of each.

Motion One provides a `stagger` function that can be used to dynamically set `delay`. First, import it:

```javascript
import { animate, stagger } from "motion"
```

Then set `delay` to `stagger(offset)`:

<InlineSandbox
  content={{
    html: `<div class="box"></div>
<div class="box"></div>
<div class="box"></div>
<div class="box"></div>
<div class="box"></div>`,
    css: `body {
  display: flex;
  justify-content: flex-start;
  align-items: flex-start;
  flex-direction: column;
  padding: 10px;
  overflow: hidden;
}
.box {
  width: 40px;
  height: 40px;
  border-radius: 10px;
  background-color: var(--splash);
  margin: 10px;
  flex-shrink: 0;
}`,
    js: `animate(
  ".box",
  { x: 200 },
  {
    delay: stagger(0.1),
    duration: 0.5,
    easing: [.22, .03, .26, 1]
  }
)`,
  }}
  showTabs={false}
/>

<!--

To make this feel even more natural, try animating with real springs:

```javascript
import { animate, stagger, spring } from "motion"
```

<InlineSandbox
  content={{
    html: `<div class="box"></div>
<div class="box"></div>
<div class="box"></div>
<div class="box"></div>
<div class="box"></div>`,
    css: `body {
  display: flex;
  justify-content: flex-start;
  align-items: flex-start;
  flex-direction: column;
  padding: 10px;
  overflow: hidden;
}
.box {
  width: 40px;
  height: 40px;
  border-radius: 10px;
  background-color: var(--splash);
  margin: 10px;
  flex-shrink: 0;
}`,
    js: `animate(
  ".box",
  { x: 200 },
  {
    delay: stagger(0.1),
    easing: spring()
  }
)`,
  }}
  showTabs={false}
/>

-->

## More to discover

<!--
This guide has covered the basics of animation in Motion One, but [`animate`](/dom/animate), [`stagger`](/dom/stagger) and [`spring`](/dom/spring) offer more options to customize your animations.

For complex animation sequences across multiple elements, check out the [`timeline` function](/dom/timeline).
-->

This guide has covered the basics of animation in Motion One, but [`animate`](/dom/animate) and [`stagger`](/dom/stagger) offer more options to customize your animations.

For even more complex animation sequences across multiple elements, check out the [`timeline` function](/dom/timeline).
