describe("Pose inheritence", () => {
  test("When parent changes, child changes", () => {})

  test("When parent hover starts, child changes", () => {})

  test("When parent press starts, child changes", () => {})

  test("When parent hover ends, child changes", () => {})

  test("When parent press ends, child changes", () => {})

  test("delayChildren as duration", () => {})

  test("delayChildren as stagger function", () => {})

  test("when: 'beforeChildren'", () => {})

  test("when: 'afterChildren'", () => {})
})
